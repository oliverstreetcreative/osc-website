// Shared offer copy for the silo sales pages (/service-businesses and the ones
// that follow). One source so every silo page says the price the same way.
//
// Sam, 2026-09-24 15:22: "Packages from $3,499 per video" - an Apple-style
// "from" price: it anchors the number and lets a visitor self-qualify. Change
// it here and every silo page follows.

export const FROM_PRICE = "$3,499"
export const FROM_PRICE_LINE = `Packages from ${FROM_PRICE} per video`
